# Staged Prerequisites

Stage these exact installer files in this folder before building:

- `vstor_redist.exe`
- `MicrosoftEdgeWebView2RuntimeInstallerX86.exe`
- `MicrosoftEdgeWebView2RuntimeInstallerX64.exe`

The bundle build fails fast when any required file is missing.
